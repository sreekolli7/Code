package main

import "fmt"

func main() {

	x := [9]int{1, 2, 4, 8, 16, 32, 64, 128} // I assigned a value to x and next to the square brackets
	// the type which is int.
	for i, v := range x { // I used i which is the index and the v is varant i used to assign to range the number
		// x is the slice and I am still not sure on what it does even after I watched the videos?
		//
		fmt.Println(i, v)
	}

	//Incomplete code

}


//Good job
