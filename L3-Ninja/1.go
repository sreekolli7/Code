package main

import "fmt"

func main() {
	for x := //assinging x to one
		1; x <= 10; // i greater or equal to 100 to get 10-10000
	x++ { //i++ is used for incrementing
		fmt.Print(x) // used to print out the output
	}
}
